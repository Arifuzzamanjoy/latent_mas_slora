# Core Dependencies
torch>=2.1.0
torchvision>=0.16.0
transformers>=4.40.0
peft>=0.10.0
accelerate>=0.27.0
bitsandbytes>=0.43.0
safetensors>=0.4.0

# Optional: vLLM for production serving
# vllm>=0.4.0  # Uncomment if using vLLM backend

# Utilities
tqdm>=4.66.0
numpy>=1.24.0
huggingface_hub>=0.21.0

# Evaluation
datasets>=2.18.0

# RAG Dependencies
sentence-transformers>=2.2.0  # For embeddings

# VLM (Vision-Language Model) Dependencies
qwen-vl-utils[decord]  # For Qwen2.5-VL image/video processing
# pypdf>=4.0.0  # Optional: PDF support
# PyPDF2>=3.0.0  # Alternative PDF support

# Training Dependencies (included in torch/transformers)
# Additional training utilities included in base packages

# Tool Use (optional dependencies)
# requests>=2.28.0  # For web fetch tool (usually pre-installed)

# Conversation Persistence (included in stdlib)
# json, pathlib, threading - all stdlib
